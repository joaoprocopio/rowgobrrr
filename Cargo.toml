[package]
name = "beecrab"
version = "0.1.0"
edition = "2024"

[profile.release]
codegen-units = 1
debug = "line-tables-only"
strip = "none"
panic = "abort"

[dependencies]
libc = "0.2.185"
rapidhash = "4.4.1"
