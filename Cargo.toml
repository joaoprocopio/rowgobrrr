[package]
name = "beecrab"
version = "0.1.0"
edition = "2024"

[profile.release]
codegen-units = 1
debug = "full" # TODO: remove this when making production release; info useful only for debugging
strip = "none"
panic = "abort"

[dependencies]
libc = "0.2.185"
